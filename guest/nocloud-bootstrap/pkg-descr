Applies Proxmox NoCloud metadata to an OPNsense virtual machine during the
first boot. It configures hostname, management networking, DNS and an optional
administrative user with independent WebUI password and key-only SSH access.
Root login remains disabled for WebUI, API and SSH.
