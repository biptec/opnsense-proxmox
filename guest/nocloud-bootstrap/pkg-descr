Applies Proxmox NoCloud metadata to an OPNsense virtual machine during the
first boot. It can preserve the image network configuration, enable DHCP, or
apply a static management address. Hostname, DNS and optional administrative
credentials are processed independently from the selected network mode. Root
login remains disabled for WebUI, API and SSH.
